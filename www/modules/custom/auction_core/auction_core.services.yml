services:
  auction_core.offers:
    class: Drupal\auction_core\Service\Offers
    arguments: ['@entity_type.manager', '@database']

  auction_core.timeline:
    class: Drupal\auction_core\Service\Timeline
    arguments: ['@entity_type.manager', '@datetime.time', '@database']

  auction_core.bidlink:
    class: Drupal\auction_core\Service\BidLinkBuilder
    arguments: ['@entity_type.manager', '@current_user', '@renderer']

  # Nieuw: soft-close functionaliteit
  auction_core.soft_close:
    class: Drupal\auction_core\Service\SoftCloseManager
    arguments:
      - '@entity_type.manager'
      - '@database'
      - '@lock'
      - '@datetime.time'
      - '@cache_tags.invalidator'
      - '@config.factory'
